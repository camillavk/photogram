<% if @posts.any? %>
  <% @posts.each do |post| %>
    <%= image_tag post.image.url(:thumb) %>
    <%= link_to "#{post.caption}", post_path(post) %>
  <% end %>
<% else %>
  No posts yet!
<% end %>

<a href='/posts/new'>New Post</a>
